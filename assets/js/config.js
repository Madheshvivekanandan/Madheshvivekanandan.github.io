/* SITE CONFIG — public, client-side settings.
 * NOTE: This is NOT a secret store. A static GitHub Pages site cannot read a
 * real .env file, and the Web3Forms access key is a PUBLIC key by design
 * (it ships to the browser on every submission). Keep only public values here.
 * To swap the contact-form key, change WEB3FORMS_ACCESS_KEY below. */
window.SITE_CONFIG = {
  WEB3FORMS_ACCESS_KEY: "63306757-c919-4499-8a2f-e6a374413bf2",
};
